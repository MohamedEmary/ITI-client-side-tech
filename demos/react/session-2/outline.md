1. Hooks?
   1. What are hooks?
   2. Where can we use hooks?
2. `useState`
   1. initial state
   2. state updater function
   3. using previous state
   4. updating objects and arrays state with spread operator
   5. Avoid mutating state directly
   6. Difference between state and regular variables
3. props and children
   1. Explain them from `createElement` function
   2. passing data between components
   3. default props value
4. conditional rendering
   1. if-else statements inside component
   2. ternary operator `condition ? true : false`
   3. using `&&` operator
      1. Always try to use it with boolean expressions. If a falsy value is encountered, it will not render the component.
   4. conditional CSS classes
5. lists and keys
   1. React's reconciliation process
   2. Don't use array index as key
6. `{}` are used to embed one line of JavaScript expression within JSX
7. Mapping arrays to components
8. **Jokes App**
9. **[Travel Journal App](https://www.figma.com/design/QG4cOExkdbIbhSfWJhs2gs/Travel-Journal)**
10. forms
    1. controlled components
    2. `onSubmit` event handler
    3. action function (prevents default behavior, gets form data, resets form)
    4. action function vs `onSubmit`
11. `useRef`
    1. accessing DOM elements
    2. storing mutable values that do not trigger re-renders
12. `useEffect`
    1. Component lifecycle (mounting, updating, unmounting)
    2. Synchronizing with **external systems** (APIs, Events, timers)
    3. setup function
    4. dependencies array (empty, non-existent, with states)
    5. cleanup function
13. **Chef AI App**
14. Intro to Routing (No Context or Protected Routes)

---

- Continue on `useEffect`
- Chef AI App
- Context API
- Routing

---

Assignment: convert Meals app we created earlier into a React app

extensions:

- `hossaini.bootstrap-intellisense`
- `Thinker.font-awesome`
- `riazxrazor.html-to-jsx`
- `Zignd.html-css-class-completion`
- `xabikos.JavaScriptSnippets`
- `ritwickdey.LiveServer`
- `esbenp.prettier-vscode`
- `WallabyJs.quokka-vscode`
- `yoelvismulen.react17plus-snippets`